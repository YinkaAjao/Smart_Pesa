name: smart_pesa
description: Smart-Pesa mobile application frontend with Flutter + BLoC

publish_to: 'none'

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^8.2.1
  equatable: ^2.0.5
  fl_chart: ^0.60.0
  google_fonts: ^6.0.0
  go_router: ^7.5.0
  

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.1

flutter:
  uses-material-design: true
